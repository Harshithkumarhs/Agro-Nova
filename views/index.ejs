<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome to FarmConnect</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css">
    <style>
        body {
            background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            min-height: 100vh;
            display: flex;
            align-items: center;
        }
        .container {
            margin: 50px auto;
        }
        .card {
            background: rgba(255, 255, 255, 0.95);
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease;
        }
        .card:hover {
            transform: translateY(-5px);
        }
        .btn {
            padding: 12px 25px;
            border-radius: 30px;
            font-weight: 600;
            letter-spacing: 0.5px;
            transition: all 0.3s ease;
        }
        .btn-consumer {
            background: linear-gradient(45deg, #007bff, #0056b3);
            color: white;
            border: none;
        }
        .btn-consumer:hover {
            background: linear-gradient(45deg, #0056b3, #004085);
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(0, 123, 255, 0.4);
        }
        .btn-logistics {
            background: linear-gradient(45deg, #28a745, #218838);
            color: white;
            border: none;
        }
        .btn-logistics:hover {
            background: linear-gradient(45deg, #218838, #1e7e34);
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(40, 167, 69, 0.4);
        }
        .btn-farmer {
            background: linear-gradient(45deg, #ffc107, #e0a800);
            color: black;
            border: none;
        }
        .btn-farmer:hover {
            background: linear-gradient(45deg, #e0a800, #d39e00);
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(255, 193, 7, 0.4);
        }
        .quote-section {
            margin-bottom: 30px;
            padding: 20px;
            font-style: italic;
            color: #495057;
        }
        .main-title {
            font-size: 2.5rem;
            font-weight: 700;
            color: #2c3e50;
            margin-bottom: 1.5rem;
            text-transform: uppercase;
            letter-spacing: 2px;
        }
        .role-description {
            font-size: 1.1rem;
            color: #6c757d;
            margin-bottom: 2rem;
        }
    </style>
</head>
<body>
    <div class="container text-center">
        <h1 class="main-title animate__animated animate__fadeInDown">Welcome to FarmConnect</h1>
        <div class="quote-section animate__animated animate__fadeIn animate__delay-1s">
            <p class="mb-0">"Agriculture is our wisest pursuit, because it will in the end contribute most to real wealth, good morals & happiness."</p>
            <small>- Thomas Jefferson</small>
        </div>
        <div class="row justify-content-center">
            <div class="col-md-8 col-lg-6">
                <div class="card p-4 animate__animated animate__fadeInUp animate__delay-1s">
                    <div class="role-description">
                        <p>Join our community and be part of the sustainable agriculture movement. Select your role to begin:</p>
                    </div>
                    <form action="/submit-role" method="POST">
                        <input type="hidden" id="role" name="role">
                        <div class="d-grid gap-3">
                            <button type="submit" class="btn btn-consumer" onclick="setRole('consumer')">
                                <i class="fas fa-shopping-cart me-2"></i>Consumer
                            </button>
                            <button type="submit" class="btn btn-logistics" onclick="setRole('logistics')">
                                <i class="fas fa-truck me-2"></i>Logistics Partner
                            </button>
                            <button type="submit" class="btn btn-farmer" onclick="setRole('farmer')">
                                <i class="fas fa-tractor me-2"></i>Farmer
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
    <script src="https://kit.fontawesome.com/your-font-awesome-kit.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        function setRole(role) {
            document.getElementById('role').value = role;
        }
    </script>
</body>
</html>
