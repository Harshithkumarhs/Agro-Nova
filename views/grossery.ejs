<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Fresh Grocery List - FarmConnect</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
  <style>
    body {
      background: linear-gradient(135deg, #43c6ac, #191654);
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      min-height: 100vh;
      padding: 40px 0;
    }
    .form-container {
      margin: 20px auto;
      max-width: 800px;
      padding: 40px;
      background: rgba(255, 255, 255, 0.95);
      border-radius: 20px;
      box-shadow: 0 15px 30px rgba(0, 0, 0, 0.2);
      animation: fadeInUp 0.8s;
    }
    .form-container h2 {
      margin-bottom: 30px;
      text-align: center;
      color: #2c3e50;
      font-weight: 700;
      font-size: 2.2rem;
      text-transform: uppercase;
      letter-spacing: 1px;
    }
    .form-container p {
      text-align: center;
      color: #666;
      margin-bottom: 30px;
    }
    .btn-primary {
      background: linear-gradient(45deg, #43c6ac, #191654);
      border: none;
      padding: 12px 25px;
      font-weight: 600;
      letter-spacing: 0.5px;
      transition: all 0.3s ease;
    }
    .btn-primary:hover {
      background: linear-gradient(45deg, #191654, #43c6ac);
      transform: translateY(-2px);
      box-shadow: 0 5px 15px rgba(67, 198, 172, 0.4);
    }
    .btn-secondary {
      background: linear-gradient(45deg, #6c757d, #495057);
      border: none;
      padding: 12px 25px;
      font-weight: 600;
      letter-spacing: 0.5px;
      transition: all 0.3s ease;
    }
    .btn-secondary:hover {
      background: linear-gradient(45deg, #495057, #6c757d);
      transform: translateY(-2px);
      box-shadow: 0 5px 15px rgba(108, 117, 125, 0.4);
    }
    .grocery-item {
      background: #ffffff;
      padding: 25px;
      border-radius: 15px;
      box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
      margin-bottom: 20px;
      border: 2px solid #e9ecef;
      transition: all 0.3s ease;
    }
    .grocery-item:hover {
      transform: translateY(-3px);
      border-color: #43c6ac;
    }
    .grocery-item .row {
      align-items: center;
    }
    label {
      font-weight: 600;
      color: #2c3e50;
      margin-bottom: 8px;
      display: block;
    }
    .form-control {
      border: 2px solid #e9ecef;
      border-radius: 10px;
      padding: 12px;
      transition: all 0.3s ease;
    }
    .form-control:focus {
      border-color: #43c6ac;
      box-shadow: 0 0 0 0.2rem rgba(67, 198, 172, 0.25);
    }
    .input-group {
      position: relative;
    }
    .input-group i {
      position: absolute;
      top: 50%;
      transform: translateY(-50%);
      left: 15px;
      color: #6c757d;
      z-index: 10;
    }
    .input-group input {
      padding-left: 40px;
    }
    .remove-item {
      color: #dc3545;
      cursor: pointer;
      transition: all 0.3s ease;
    }
    .remove-item:hover {
      color: #c82333;
      transform: scale(1.1);
    }
    .btn-link {
      color: #43c6ac;
      text-decoration: none;
      font-weight: 600;
      transition: all 0.3s ease;
    }
    .btn-link:hover {
      color: #191654;
      text-decoration: none;
    }
    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
    .quote-section {
      text-align: center;
      color: white;
      margin-bottom: 30px;
      animation: fadeIn 1s;
    }
    .quote-text {
      font-style: italic;
      font-size: 1.2rem;
      margin-bottom: 10px;
    }
    .quote-author {
      font-size: 0.9rem;
      opacity: 0.8;
    }
  </style>
</head>
<body>

<div class="container">
  <div class="quote-section animate__animated animate__fadeIn">
    <p class="quote-text">"Fresh ingredients make all the difference in creating healthy, delicious meals."</p>
    <p class="quote-author">- Farm to Table Movement</p>
  </div>
  
  <div class="form-container animate__animated animate__fadeInUp">
    <h2>Fresh Grocery List</h2>
    <p>Add your grocery items below and we'll make sure they're fresh from the farm to your table.</p>
    
    <form action="/submit-grocery" method="POST">
      <div class="row mb-4">
        <div class="col-md-6">
          <div class="mb-3">
            <label for="apartmentId" class="form-label">Apartment ID</label>
            <div class="input-group">
              <i class="fas fa-building"></i>
              <input type="text" class="form-control" id="apartmentId" name="apartmentId" placeholder="Enter Apartment ID" required>
            </div>
          </div>
        </div>
        <div class="col-md-6">
          <div class="mb-3">
            <label for="houseId" class="form-label">House ID</label>
            <div class="input-group">
              <i class="fas fa-home"></i>
              <input type="text" class="form-control" id="houseId" name="houseId" placeholder="Enter House ID" required>
            </div>
          </div>
        </div>
      </div>

      <div id="groceryItems">
        <div class="grocery-item animate__animated animate__fadeIn">
          <div class="row">
            <div class="col-md-5">
              <label for="item1" class="form-label">Item</label>
              <div class="input-group">
                <i class="fas fa-shopping-basket"></i>
                <input type="text" class="form-control" id="item1" name="groceryItems[0][item]" placeholder="Enter item name" required>
              </div>
            </div>
            <div class="col-md-5">
              <label for="quantity1" class="form-label">Quantity</label>
              <div class="input-group">
                <i class="fas fa-balance-scale"></i>
                <input type="number" class="form-control" id="quantity1" name="groceryItems[0][quantity]" placeholder="Enter quantity" required>
              </div>
            </div>
            <div class="col-md-2 d-flex align-items-end">
              <div class="remove-item mt-3 text-center w-100" style="display: none;">
                <i class="fas fa-trash-alt"></i>
              </div>
            </div>
          </div>
        </div>
      </div>

      <button type="button" class="btn btn-secondary mb-4 w-100" id="addItemButton">
        <i class="fas fa-plus me-2"></i>Add More Items
      </button>

      <button type="submit" class="btn btn-primary w-100 mb-3">
        <i class="fas fa-paper-plane me-2"></i>Submit List
      </button>

      <div class="text-center">
        <a href="/generate-total-grocery" class="btn btn-link">
          <i class="fas fa-list-alt me-2"></i>Generate Total Grocery List
        </a>
      </div>
    </form>
  </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
<script>
document.addEventListener('DOMContentLoaded', function() {
    const groceryItems = document.getElementById('groceryItems');
    const addItemButton = document.getElementById('addItemButton');
    let itemCount = 1;

    addItemButton.addEventListener('click', function() {
        itemCount++;
        const newItem = document.createElement('div');
        newItem.className = 'grocery-item animate__animated animate__fadeIn';
        newItem.innerHTML = `
            <div class="row">
                <div class="col-md-5">
                    <label for="item${itemCount}" class="form-label">Item</label>
                    <div class="input-group">
                        <i class="fas fa-shopping-basket"></i>
                        <input type="text" class="form-control" id="item${itemCount}" 
                               name="groceryItems[${itemCount-1}][item]" placeholder="Enter item name" required>
                    </div>
                </div>
                <div class="col-md-5">
                    <label for="quantity${itemCount}" class="form-label">Quantity</label>
                    <div class="input-group">
                        <i class="fas fa-balance-scale"></i>
                        <input type="number" class="form-control" id="quantity${itemCount}" 
                               name="groceryItems[${itemCount-1}][quantity]" placeholder="Enter quantity" required>
                    </div>
                </div>
                <div class="col-md-2 d-flex align-items-end">
                    <div class="remove-item mt-3 text-center w-100">
                        <i class="fas fa-trash-alt"></i>
                    </div>
                </div>
            </div>`;
        groceryItems.appendChild(newItem);
        
        // Show all remove buttons if there's more than one item
        const removeButtons = document.querySelectorAll('.remove-item');
        removeButtons.forEach(button => {
            button.style.display = 'block';
        });

        // Add click event to the new remove button
        const removeButton = newItem.querySelector('.remove-item');
        removeButton.addEventListener('click', function() {
            newItem.classList.add('animate__fadeOut');
            setTimeout(() => {
                newItem.remove();
                // Hide the last remove button if only one item remains
                const remainingItems = document.querySelectorAll('.grocery-item');
                if (remainingItems.length === 1) {
                    remainingItems[0].querySelector('.remove-item').style.display = 'none';
                }
            }, 500);
        });
    });
});
</script>
</body>
</html>
